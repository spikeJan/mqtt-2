# 管道探测小车 - 手机端数据展板 使用说明书

---

## 一、快速预览

| 场景 | 推荐方式 | 耗时 |
|------|----------|------|
| 只想看展板 (没有小车) | `python launcher.py` | 10秒 |
| 连接真实小车 | 开两个 CMD 窗口 (详见第四节) | 30秒 |
| 手机上打开展板 | 浏览器输入 IP:8080 | 10秒 |

---

## 二、需要配置的环境

### 2.1 源码模式 (开发者)

| 环境 | 版本要求 | 用途 |
|------|----------|------|
| Python | ≥ 3.10 | 运行桥接、分析工具、一体化启动器 |
| Node.js + npm | ≥ 18 | 编译 Vue 前端 (`npm run build`) |
| Mosquitto | 2.x (便携版已自带) | MQTT Broker |
| FFmpeg | 建议 6.x+ | RTSP摄像头拉流转码 |

**Python 包** (桥接程序用):

```bash
pip install paho-mqtt pyyaml
```

**分析工具用**:

```bash
pip install openpyxl
```

**FFmpeg 安装** (摄像头需要):

1. 打开 https://ffmpeg.org/download.html → Windows → gyan.dev → 下载 `ffmpeg-release-essentials.zip`
2. 解压到 `C:\ffmpeg\`
3. 将 `C:\ffmpeg\bin` 添加到系统 PATH 环境变量
4. 新开 CMD 执行 `ffmpeg -version` 验证

### 2.2 手机端

- 任何现代手机浏览器 (Safari / Chrome / 微信内置浏览器)
- 手机和电脑在**同一个局域网** (连同一个WiFi 或手机热点)

---

## 三、文件夹结构

```
项目根目录/
├── mosquitto/                    ← 便携版 MQTT Broker (即开即用)
│   ├── mosquitto.exe             # MQTT Broker 主程序
│   ├── mosquitto_portable.conf   # 监听 1884(MQTT) + 9001(WebSocket)
│   └── *.dll                     # 运行所需 7 个 DLL (已自带)
│
├── dashboard/                    ← Vue3 手机展板 (PWA)
│   ├── dist/                     # 已构建好的静态文件
│   ├── src/                      # Vue 源码 (开发用)
│   ├── package.json              # npm 依赖清单
│   └── node_modules/             # npm 已安装包
│
├── bridge/                       ← 数据桥接 (TCP→MQTT+摄像头+网页)
│   ├── main.py                   # 桥接主程序 (处理帧类型0x01/0x02/0x04)
│   ├── config.yaml               # 小车IP/端口/MQTT/摄像头 配置
│   ├── network_reader.py         # TCP帧解析 (支持3种帧类型)
│   ├── mqtt_publisher.py         # MQTT发布 (8个传感器字段)
│   ├── db_writer.py              # SQLite写入
│   └── rtsp_streamer.py          # FFmpeg RTSP→MJPEG + 静态网页服务
│
├── database/
│   ├── schema.sql                # 数据库建表脚本
│   └── car_data.db               # 运行时生成的数据
│
├── analysis/                     ← 数据分析与导出
│   ├── analyze.py                # 统计报告
│   ├── export_csv.py             # 导出 CSV / Excel
│   └── report_generator.py       # 导出 HTML 报告
│
├── launcher.py                   ← 一体化启动器 (模拟模式)
├── 启动数据桥接.bat              # 快捷启动桥接 (模拟/真实)
├── 启动手机展板.bat              # 快捷启动 Vue dev server
├── 项目说明.md                   # 技术文档
├── 使用说明书.md                 # 本文档
└── .gitignore
```

---

## 四、启动上位机

### 方式一: 模拟模式 (一键启动，无需小车)

```bash
cd /d D:\桌面\Desktop\mqtt手机端2 && python launcher.py
```

自动完成:
1. 启动 Mosquitto MQTT Broker (端口 1884 + WebSocket 9001)
2. 启动 HTTP 静态文件服务 (端口 8080)
3. 启动模拟数据桥接 (每 0.1 秒生成传感器数据)
4. 自动打开浏览器 → `http://localhost:8080`

### 方式二: 真实模式 (连接小车)

**前置条件：**
- 小车通电，网线连接电脑以太网口
- 电脑以太网 IP 设为 `192.168.0.100`（子网掩码 `255.255.255.0`）
- `bridge/config.yaml` 中小车 IP 为 `192.168.0.233:1234`

**开两个 CMD 窗口**（`Win + R` → `cmd`，重复两次）：

```
窗口1: cd /d D:\桌面\Desktop\mqtt手机端2\mosquitto && mosquitto.exe -c mosquitto_portable.conf
窗口2: cd /d D:\桌面\Desktop\mqtt手机端2\bridge && python main.py
```

窗口2 会同时提供：
- TCP 帧解析 → MQTT 发布（传感器数据）
- RTSP 摄像头 → MJPEG 流（需 FFmpeg）
- 静态网页服务（8080 端口，无需单独 HTTP 服务）

### 方式三: 分体调试 (开发)

```bash
# 窗口1: MQTT Broker
cd /d D:\桌面\Desktop\mqtt手机端2\mosquitto && mosquitto.exe -c mosquitto_portable.conf

# 窗口2: 手机展板 (Vue dev server, 端口 5173)
cd /d D:\桌面\Desktop\mqtt手机端2\dashboard && npm run dev

# 窗口3: 数据桥接
cd /d D:\桌面\Desktop\mqtt手机端2\bridge && python main.py
```

---

## 五、手机上查看展板

### 前置条件

手机和电脑必须连接**同一个 Wi-Fi** 或电脑开热点让手机连。

### 5.1 获取电脑 IP

Windows CMD 中执行:

```bash
ipconfig | findstr "192.168"
```

找到 Wi-Fi/WLAN 适配器下的 `IPv4 地址`，一般为 `192.168.x.x`。

### 5.2 手机浏览器打开

```
http://你的电脑IP:8080
```

例如: `http://192.168.43.46:8080`

### 5.3 添加到手机主屏幕 (推荐)

- **iPhone Safari**: 点底部分享按钮 → "添加到主屏幕"
- **Android Chrome**: 点右上角 `⋮` → "添加到主屏幕"

添加后像原生 App，可以全屏运行。

### 5.4 如果连接不上

| 问题 | 检查 |
|------|------|
| 页面打不开 | Windows 防火墙是否拦截? 添加 8080 TCP 入站规则 |
| 数据不更新、MQTT红色 | 添加 9001 TCP 入站规则 (WebSocket) |
| 摄像头无画面 | 确认 FFmpeg 已安装 (`ffmpeg -version`)；添加 8080 入站规则 |
| 手机找不到电脑 | 确认同一 Wi-Fi；公共 Wi-Fi 可能 AP 隔离，改用手机热点 |

### 5.5 防火墙规则添加方法

`Win + R` → `wf.msc` → 入站规则 → 新建规则 → 端口 → TCP → 输入端口号 → 允许连接 → 三个勾全选 → 命名 → 完成。

需添加的端口: **8080** (网页+摄像头)

---

## 六、配置说明

### 6.1 小车连接配置

编辑 `bridge/config.yaml`:

```yaml
network:
  host: 192.168.0.233     # 小车网口IP
  port: 1234              # 数据端口

camera:
  rtsp_url: rtsp://192.168.0.233/live/0    # 摄像头RTSP地址
  mjpeg_port: 8080        # MJPEG+网页服务端口
  fps: 8                  # 抓帧频率

mqtt:
  broker: localhost
  port: 1884              # 必须与 mosquitto_portable.conf 一致
  topic_prefix: car
```

### 6.2 传感器字段 (8个)

Roll（翻滚角）、Pitch（俯仰角）、Yaw（偏航角）、Speed（速度）、Servo1（舵机1）、Servo2（舵机2）、CO（一氧化碳 ppm）、CO2（二氧化碳 ppm）

---

## 七、端口一览

| 端口 | 协议 | 用途 |
|------|------|------|
| 1884 | MQTT TCP | Mosquitto Broker (桥接发布到这里) |
| 9001 | WebSocket | 手机浏览器→MQTT (Mosquitto 原生) |
| 8080 | HTTP | 静态网页 + MJPEG摄像头流 (桥接统一提供) |
| 5173 | HTTP | Vue 开发服务器 (仅开发时用) |
| 1234 | TCP | 小车数据源 (仅连接真实小车时用) |
