# 管道探测小车 - 手机端数据展板 项目说明

## 一、系统架构

```
小车(STM32) ──TCP:1234──> PC桥接 ──MQTT:1884──> Mosquitto ──WS:9001──> 手机浏览器
                (Python)                              │
                          ┌───────────────────────────┘
                          │
                    SQLite 数据库
```

### 三层结构

| 层 | 技术 | 作用 |
|----|------|------|
| 数据桥接 | Python (paho-mqtt, socket, pyyaml) | TCP连接小车, 解析二进制帧, 发布MQTT |
| 消息中转 | Mosquitto (原生WebSocket) | MQTT Broker + WebSocket |
| 前端展板 | Vue3 + Vant4 + ECharts + mqtt.js | 手机PWA网页, 5个页面 |

### 数据流说明

1. 小车通过TCP发送二进制帧（RS485协议v2格式）
2. Python桥接解析帧 → 发布JSON到MQTT主题 `car/sensors/all`
3. 同时写入SQLite数据库 (`database/car_data.db`)
4. 前端通过WebSocket连接MQTT, 订阅主题实时展示

---

## 二、二进制帧协议 (RS485 v2)

帧头统一为 `0xAA 0x55`，按帧类型分为三种：

### 帧类型 0x04 — 全量传感器帧（主要使用）

| 字节 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 0-1 | 帧头 | uint8×2 | `AA 55` |
| 2 | 序列号 | uint8 | 0~255 循环计数 |
| 3 | 帧类型 | uint8 | `0x04` = 全数据帧 |
| 4-7 | Roll | float32 | 翻滚角，小端序 |
| 8-11 | Pitch | float32 | 俯仰角，小端序 |
| 12-15 | Yaw | float32 | 偏航角，小端序 |
| 16-17 | Speed | uint16 | 当前速度 0-80 |
| 18-19 | Servo1 | uint16 | 舵机1角度 0-180° |
| 20-21 | Servo2 | uint16 | 舵机2角度 0-180° |
| 22-23 | CO | uint16 | 一氧化碳 ppm |
| 24-25 | CO2 | uint16 | 二氧化碳 ppm |
| 26 | 校验和 | uint8 | 字节2-25累加取低8位 |

> **总长 27 字节，无帧尾。** 校验范围为 seq(1) + type(1) + payload(22) = 24 字节。

### 帧类型 0x01 — 姿态包

帧头(2) + seq(1) + 0x01(1) + float32×3(12) + status(2) + 校验和(1) + 帧尾 `DD EE`(2) = 21 字节

### 帧类型 0x02 — 轮子状态包

帧头(2) + seq(1) + 0x02(1) + int32×2(8) + 校验和(1) + 帧尾 `DD EE`(2) = 15 字节

---

## 三、手机展板 (Vue3 PWA)

### 技术栈

- **Vue 3** + Vite 构建
- **Vant 4** 移动端UI组件库
- **ECharts 5** 图表 (仪表盘/折线图)
- **mqtt.js** MQTT客户端 (WebSocket)
- **PWA** 可添加到手机主屏幕

### 5个页面

| 页面 | 文件 | 功能 |
|------|------|------|
| 仪表盘 | `Dashboard.vue` | 8个传感器卡片 + 速度仪表盘 + 告警横幅 |
| 曲线图 | `SensorCharts.vue` | Roll/Pitch/Yaw/Speed/CO/CO2时间序列图 |
| 视频 | `VideoFeed.vue` | MJPEG视频流 + 云台角度 + 抓图 |
| 导出 | `DataExport.vue` | 按时间范围导出CSV/Excel |
| 分析 | `Analysis.vue` | 统计摘要 + 异常检测 + 维修建议 |

### 数据字段

| 字段 | 类型 | 说明 |
|------|------|------|
| Roll | float32 | 翻滚角 |
| Pitch | float32 | 俯仰角 |
| Yaw | float32 | 偏航角 |
| Speed | uint16 | 速度 (0-80) |
| Servo1 | uint16 | 舵机1角度 (0-180°) |
| Servo2 | uint16 | 舵机2角度 (0-180°) |
| CO | uint16 | 一氧化碳 (ppm) |
| CO2 | uint16 | 二氧化碳 (ppm) |

### 文件结构

```
dashboard/
├── src/
│   ├── views/          # 5个页面
│   ├── components/     # SpeedGauge, SensorCard, AlertBanner, ChartPanel
│   ├── services/mqtt.js  # MQTT连接封装
│   └── assets/main.css
├── dist/               # 构建产物 (静态HTML/JS/CSS)
├── package.json
└── vite.config.js      # PWA配置
```

---

## 四、桥接程序 (bridge/)

### 文件说明

| 文件 | 作用 |
|------|------|
| `main.py` | 主入口，协调所有模块，处理帧类型 0x01/0x02/0x04 |
| `network_reader.py` | TCP帧接收与解析，支持三种帧类型自动识别 |
| `mqtt_publisher.py` | MQTT发布，聚合+单项双通道 |
| `db_writer.py` | SQLite数据库写入 |
| `rtsp_streamer.py` | RTSP→MJPEG转码（FFmpeg），同时提供静态网页服务 |
| `config.yaml` | 小车IP、端口、摄像头、MQTT等配置 |

### 摄像头方案

使用 **FFmpeg** 命令行（非 OpenCV）拉 RTSP 流转码为 MJPEG：

```
ffmpeg -rtsp_transport tcp -i rtsp://小车IP/live/0 -f image2pipe -vcodec mjpeg -q:v 5 -r 8 -
```

MJPEG HTTP 服务器同时提供静态网页文件，统一在 8080 端口。

---

## 五、打包为单个 .exe 进度

### 目标

将整个系统打包成一个 `PipeCarDashboard.exe`，拷贝到任何Windows电脑双击即用，不需要安装 Python/Node.js/Mosquitto。

### 架构

```
PipeCarDashboard.exe
├── 内置 Mosquitto (子进程, 端口1884 MQTT + 端口9001 WebSocket原生)
├── 内置 HTTP 静态文件服务 (端口8080, serve Vue dist/)
├── 内置 数据桥接 (模拟模式, 生成虚拟传感器数据)
└── 自动打开浏览器 → http://localhost:8080
```

### 已完成

- [x] Vue前端构建为静态文件 (`dashboard/dist/`)
- [x] Mosquitto.exe 及完整配套 DLL 已复制到 `mosquitto/`
- [x] `launcher.py` 一体化启动器 (Python源码)
- [x] `mosquitto_portable.conf` 同时开启 MQTT(1884) 和 WebSocket(9001)
- [x] 端口清理、数据库写入、文件日志
- [x] 安装打包依赖 (pyinstaller, paho-mqtt)
- [x] `python launcher.py` 本地源码测试通过
- [x] PyInstaller 打包成功, `dist/PipeCarDashboard.exe`
- [x] exe 端到端测试通过: 1884/9001/8080 三端口均正常监听

### PyInstaller 打包命令

```bash
python -m PyInstaller --onefile --name "PipeCarDashboard" ^
  --add-data "dashboard/dist;dashboard/dist" ^
  --add-data "mosquitto;mosquitto" ^
  --add-data "database/schema.sql;database/" ^
  --hidden-import paho.mqtt.client ^
  --hidden-import sqlite3 ^
  --clean launcher.py
```

### Mosquitto DLL 清单

| 文件 | 来源 |
|------|------|
| mosquitto.exe | `C:\Program Files\Mosquitto\` |
| mosquitto.dll, mosquitto_common.dll, cjson.dll | 同上 |
| libcrypto-3-x64.dll, libssl-3-x64.dll | 同上 |
| libmicrohttpd-dll.dll, pthreadVC3.dll | 同上 |

### 待完成

- [ ] 在另一台电脑测试 .exe
- [ ] (可选) 真实模式: 通过配置文件切换 TCP 连接小车

---

## 六、运行方式

### 模拟模式 (一键启动)

```bash
python launcher.py
```

或双击 `dist/PipeCarDashboard.exe`。自动启动 Mosquitto + HTTP + 虚拟传感器数据。

### 真实模式 (连接小车)

只需两个 CMD 窗口：

```
窗口1: cd /d D:\桌面\Desktop\mqtt手机端2\mosquitto && mosquitto.exe -c mosquitto_portable.conf
窗口2: cd /d D:\桌面\Desktop\mqtt手机端2\bridge && python main.py
```

桥接程序同时提供：
- TCP 帧解析 → MQTT 发布
- RTSP 摄像头 → MJPEG 流
- 静态网页服务 (8080 端口)
- SQLite 数据库写入

### 分体调试模式 (开发)

```bash
# 窗口1: MQTT Broker
cd mosquitto && mosquitto.exe -c mosquitto_portable.conf

# 窗口2: 手机展板 (Vue dev server)
cd dashboard && npm run dev

# 窗口3: 数据桥接
cd bridge && python main.py
```
