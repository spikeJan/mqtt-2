# 管道探测小车 - 手机端数据展板 项目说明

## 一、系统架构

```
小车(STM32) ──TCP:1234──> PC桥接 ──MQTT:1884──> Mosquitto ──WS:9001──> 手机浏览器
                (Python)                              │
                          ┌───────────────────────────┘
                          │
                    SQLite 数据库
```

### 三层结构

| 层 | 技术 | 作用 |
|----|------|------|
| 数据桥接 | Python (paho-mqtt, socket, tkinter) | TCP连接小车, 解析二进制帧, 发布MQTT |
| 消息中转 | Mosquitto (原生WebSocket) | MQTT Broker + WebSocket |
| 前端展板 | Vue3 + Vant4 + ECharts + mqtt.js | 手机PWA网页, 5个页面 |

### 数据流

1. 小车 STM32 通过 TCP 发送 29 字节二进制帧
2. Python 桥接解析帧 → 发布 JSON 到 MQTT 主题 `car/sensors/all`
3. 同时写入 SQLite 数据库
4. 前端通过 WebSocket 连接 MQTT，订阅主题实时展示

---

## 二、二进制帧协议

帧头 `AA 55`，帧尾 `0D 0A`，总长 29 字节，小端序。

| 字节 | 字段 | 类型 | 说明 |
|------|------|------|------|
| 0-1 | 帧头 | uint8×2 | `AA 55` |
| 2 | 序列号 | uint8 | 0~255 循环 |
| 3 | 帧类型 | uint8 | `0x04` = 全数据帧 |
| 4-7 | Roll | float32 | 翻滚角 |
| 8-11 | Pitch | float32 | 俯仰角 |
| 12-15 | Yaw | float32 | 偏航角 |
| 16-17 | Speed | uint16 | 速度 (0-80) |
| 18-19 | Servo1 | uint16 | 舵机1 (0-180°) |
| 20-21 | Servo2 | uint16 | 舵机2 (0-180°) |
| 22-23 | CO | uint16 | 一氧化碳 (ppm) |
| 24-25 | CO2 | uint16 | 二氧化碳 (ppm) |
| 26 | 校验和 | uint8 | 字节2-25累加取低8位 |
| 27-28 | 帧尾 | uint8×2 | `0D 0A` |

> 校验范围 24 字节 (seq + type + payload)。launcher 解析时会验证校验和与帧尾。

补充帧类型（bridge/main.py 支持，exe 未使用）：

| 类型 | 数据 | 帧尾 | 总长 |
|------|------|------|------|
| 0x01 | Roll(4)+Pitch(4)+Yaw(4)+status(2) | DD EE | 21B |
| 0x02 | Speed(4)+Servo(4) | DD EE | 15B |

---

## 三、一键启动 (Exe)

### 两个独立 Exe

| 文件 | 模式 | 数据源 |
|------|------|--------|
| `dist/PipeCarDashboard.exe` | 真实模式 | TCP 连接小车 `192.168.0.233:1234` |
| `dist/PipeCarDashboard_sim.exe` | 模拟模式 | 随机生成传感器数据 |

双击后自动完成：

1. 启动 Mosquitto MQTT Broker (端口 1884 + WebSocket 9001)
2. 初始化 SQLite 数据库
3. 启动数据桥接（真实模式：TCP 连小车；模拟模式：生成随机数据）
4. 启动 HTTP 静态文件服务 (端口 8080)
5. 弹出信息窗口显示访问地址和服务状态
6. 自动打开浏览器 → `http://localhost:8080`

信息窗口实时显示：

- 本机访问 / 手机访问 URL
- MQTT Broker 状态
- 小车连接状态（真实模式：已连接 / 未连接 / 断开）
- 一键打开仪表盘 / 停止服务

### 构建命令

```bash
# 真实模式
python -m PyInstaller PipeCarDashboard.spec --distpath dist --workpath build --noconfirm

# 模拟模式
python -m PyInstaller PipeCarDashboard_sim.spec --distpath dist --workpath build_sim --noconfirm
```

### Exe 内嵌资源

```
PipeCarDashboard.exe
├── launcher.py           → 主启动逻辑 (tkinter GUI + TCP桥接)
├── dashboard/dist/       → Vue3 静态前端文件
├── mosquitto/            → 便携 MQTT Broker (exe + 7 DLL)
└── database/schema.sql   → 数据库建表
```

---

## 四、项目结构

```
mqtt手机端2/
│
├── launcher.py              # 真实模式启动器源码
├── launcher_sim.py          # 模拟模式启动器源码
├── PipeCarDashboard.spec    # 真实模式 PyInstaller 打包配置
├── PipeCarDashboard_sim.spec# 模拟模式 PyInstaller 打包配置
│
├── dashboard/               # Vue3 手机端仪表盘 (PWA)
│   ├── src/
│   │   ├── views/           # Dashboard / SensorCharts / VideoFeed / DataExport / Analysis
│   │   ├── components/      # SpeedGauge / SensorCard / AlertBanner / ChartPanel
│   │   └── services/mqtt.js # MQTT WebSocket 连接封装
│   ├── dist/                # Vite 构建产物 (exe 内置 HTTP 直接 serve)
│   ├── public/              # PWA 图标 / manifest / service-worker
│   ├── package.json
│   └── vite.config.js
│
├── bridge/                  # 独立桥接程序（开发用，功能更完整）
│   ├── main.py              # 主入口，模拟/真实两种模式 + RTSP摄像头
│   ├── network_reader.py    # TCP 帧接收，支持三种帧类型自动识别
│   ├── mqtt_publisher.py    # MQTT 发布（聚合 + 单字段双通道）
│   ├── db_writer.py         # SQLite 数据库写入
│   ├── rtsp_streamer.py     # RTSP → MJPEG 转码 (FFmpeg)
│   └── config.yaml          # 小车IP、端口、摄像头、MQTT 配置
│
├── mosquitto/               # 便携版 MQTT Broker
│   ├── mosquitto.exe
│   ├── mosquitto_portable.conf  # MQTT(1884) + WebSocket(9001)，匿名访问
│   └── *.dll                # 7 个运行时依赖
│
├── stm32/                   # 小车端 STM32F405 固件参考代码
│   ├── car_tcp_sender.c     # LwIP TCP Server，29 字节帧发送
│   └── car_tcp_sender.h     # 帧结构定义 + 网络配置
│
├── database/
│   └── schema.sql           # sensor_readings + alerts 建表语句
│
├── analysis/                # 离线数据分析工具
│   ├── analyze.py
│   ├── export_csv.py
│   └── report_generator.py
│
├── .gitignore               # Python/Node/DB/IDE/日志/env 全面覆盖
├── LICENSE
├── 使用说明书.md
└── 项目说明.md
```

---

## 五、前端仪表盘 (Vue3 PWA)

### 技术栈

- **Vue 3** + Vite 构建
- **Vant 4** 移动端 UI 组件库
- **ECharts 5** 图表（仪表盘/折线图）
- **mqtt.js** MQTT WebSocket 客户端
- **PWA** 可添加到手机主屏幕

### 5 个页面

| 页面 | 功能 |
|------|------|
| 仪表盘 | 8 个传感器卡片 + 速度仪表盘 + 告警横幅 |
| 曲线图 | Roll/Pitch/Yaw/Speed/CO/CO2 时间序列 |
| 视频 | MJPEG 视频流 + 云台角度 |
| 导出 | 按时间范围导出 CSV |
| 分析 | 统计摘要 + 异常检测 |

---

## 六、开发调试

### 启动方式对比

| 方式 | 适用场景 | 操作 |
|------|----------|------|
| exe 真实模式 | 实际使用 | 双击 `PipeCarDashboard.exe` |
| exe 模拟模式 | 快速预览 / 测试 | 双击 `PipeCarDashboard_sim.exe` |
| 源码真实模式 | 开发调试 | `python launcher.py` |
| 源码模拟模式 | 开发调试 | `python launcher_sim.py` |
| 分体调试 | 深度开发 | 三个窗口分别启动 MQTT / Vue / Bridge |

### 分体调试命令

```bash
# 窗口1: MQTT Broker
cd mosquitto && mosquitto.exe -c mosquitto_portable.conf

# 窗口2: 手机展板 (Vue dev server, 端口 5173)
cd dashboard && npm run dev

# 窗口3: 数据桥接 (端口 8080)
cd bridge && python main.py
```

### 依赖

| 模块 | 依赖 |
|------|------|
| launcher | paho-mqtt, sqlite3, tkinter (均内置于 Python) |
| bridge | paho-mqtt, sqlite3, pyyaml, ffmpeg (RTSP 需要) |
| dashboard | Node.js, npm |
| analysis | pandas, openpyxl |
